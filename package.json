{
  "name": "nua-studio-web",
  "version": "2.0.0",
  "description": "NUA STUDIO Real-time Subtitle System",
  "main": "socket-server.js",
  "scripts": {
    "start": "node socket-server.js",
    "dev": "nodemon socket-server.js || node socket-server.js",
    "test": "node diagnostic.js",
    "health": "curl http://localhost:${PORT:-3000}/health",
    "postinstall": "npm rebuild better-sqlite3 || true"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.8.1",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "better-sqlite3": "^9.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
